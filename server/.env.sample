###############################################
# Environment configuration for HerCycle server
#
# How to use:
# 1) Copy this file to `.env` in the same folder (server/.env).
# 2) Replace placeholder values with your real secrets locally ONLY.
# 3) Never commit `.env` to git. This repo's .gitignore already ignores it.
#
# Security notes:
# - If any API key is ever exposed/shared, rotate it immediately in the provider console
#   (OpenAI/Azure/OpenRouter) and update your local `.env` with the new key.
# - On Windows PowerShell, you can temporarily set variables for a single session using:
#     $env:OPENAI_API_KEY = "your-key-here"
#   but for this project, prefer putting them in `server/.env` for the Node server to read.
###############################################

# Client origin (Vite dev server)
CLIENT_ORIGIN=http://localhost:5173

# Mongo
MONGO_URI=mongodb://127.0.0.1:27017/hercycle

# Auth
JWT_SECRET=replace_me

# AI provider selection: openai | azure | openrouter
LLM_PROVIDER=openai

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# Azure OpenAI (if using azure)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini

# OpenRouter (if using openrouter)
OPENROUTER_API_KEY=

# Google Gemini (AI Studio key)
# Obtain from https://aistudio.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=
# Optional: pick a Gemini model; defaults to gemini-1.5-flash
GEMINI_MODEL=gemini-1.5-flash
